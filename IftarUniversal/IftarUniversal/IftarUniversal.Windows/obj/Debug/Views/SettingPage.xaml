<storeApps:VisualStateAwarePage
    x:Class="IftarUniversal.Views.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IftarUniversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:storeApps="using:Microsoft.Practices.Prism.StoreApps"
    xmlns:mvvm="using:Microsoft.Practices.Prism.Mvvm"  
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    xmlns:dd="using:IftarUniversal.DummyData"
    d:DataContext="{d:DesignInstance dd:SettingPageVMDummy, IsDesignTimeCreatable=True}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Button Content="Back" VerticalAlignment="Top" HorizontalAlignment="Left" Command="{Binding BackCommand}" Margin="12,12"></Button>
        
        <ListView ItemsSource="{Binding MenuList}" Grid.Row="1" Margin="12,12,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="12,8,0,0"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid> 
</storeApps:VisualStateAwarePage>

